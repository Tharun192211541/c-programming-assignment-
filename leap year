#include<stdio.h>
int main ()
{
	printf("reg.no:192211541");
	int y;
	printf("enter the year:");
	scanf("%d",&y);
	if (y%4==0)
	printf("%d is leap year",y);
	else
	printf("%d is not leap year",y);
	return 0;
}
